# ScreenPin (macOS) 기획서 v0.1

## 1) 목표

- macOS에서 스크린샷을 찍은 뒤, 결과 이미지를 **즉시 화면에 Pin(항상 위 플로팅)** 해서 참고할 수 있게 한다.
- 개발/디자인/문서 작업 중 “잠깐 참고”를 위해 파일을 열고 닫는 오버헤드를 제거한다.
- 빠른 조작(글로벌 단축키) + 가벼운 UX를 최우선으로 한다.

## 2) 핵심 사용자 시나리오

1. 사용자가 단축키(예: ⌥⌘2)를 누른다.
2. 커서가 십자 형태로 바뀌고, 사용자가 영역을 드래그해 캡처한다.
3. 캡처 결과가 즉시 작은 플로팅 창으로 뜨고(항상 위), 화면에 고정된다.
4. 사용자는 필요하면:
   - 여러 장을 동시에 Pin
   - 크기 조절/이동
   - 투명도 조절
   - 특정 Pin 닫기 또는 전체 닫기
   - 클립보드로 복사/저장
5. 작업이 끝나면 단축키 한 번으로 모든 Pin을 제거한다.

## 3) 범위 정의

### In-scope (MVP)

- 영역 캡처(드래그 선택) 후 즉시 Pin 창 생성
- Pin 창: 이동/리사이즈/닫기
- 항상 위(Topmost) 유지
- 다중 Pin(여러 창) 지원
- 글로벌 단축키 2개:
  - 캡처 & Pin
  - 전체 Pin 닫기
- Pin 이미지 복사(클립보드)
- 기본 설정(단축키, 기본 그림자/테두리, 기본 투명도)

### Out-of-scope (초기 제외)

- 동영상/움짤 캡처
- 클라우드 동기화
- 주석(annotate) 편집툴(화살표/박스 등)
- OCR/텍스트 추출
- 히스토리 갤러리(Shottr/CleanShot급)

## 4) 제품 컨셉 & 차별점

- “캡처 앱”이 아니라 “**Pin 앱**”에 집중한다.
- 기능은 최소화하되, Pin/정렬/투명도/고정의 UX 완성도를 높인다.
- 앱이 무겁지 않고 즉시 반응(핫키 → 오버레이 → Pin).

## 5) UX / UI 설계

### 5.1 캡처 오버레이

- 전체 화면에 반투명 딤 처리
- 드래그 영역에 크기 표시(예: 812×406)
- ESC: 취소
- Enter: 마지막 영역 재사용 캡처(옵션)

### 5.2 Pin 윈도우(플로팅)

- 기본 UI:
  - 상단에 최소한의 컨트롤(hover 시에만 표시)
  - 버튼: Copy / Save / Close
- 조작:
  - 드래그로 이동
  - 모서리 드래그로 리사이즈
  - 마우스 휠 + modifier로 확대/축소(옵션)
- 보기:
  - 투명도 슬라이더(또는 ⌥ + 휠)
  - 테두리 on/off
  - 그림자 on/off

### 5.3 다중 Pin 관리

- “모두 닫기” 단축키
- Pin 간 전환(옵션): 다음/이전 Pin 단축키
- 자동 정렬(옵션): 화면 우측에 세로 스택

## 6) 기능 요구사항 (PRD)

### 필수(F0)

- F0-1: 영역 캡처 후 Pin 생성
- F0-2: Pin 항상 위 유지
- F0-3: Pin 이동/리사이즈/닫기
- F0-4: 다중 Pin 동시 사용
- F0-5: 글로벌 단축키(캡처&핀 / 전체닫기)
- F0-6: 클립보드 복사

### 권장(F1)

- F1-1: Pin 투명도 조절
- F1-2: 멀티 디스플레이에서 정상 동작
- F1-3: “마지막 캡처 영역 재사용”
- F1-4: 저장(기본 위치/파일명 규칙)

### 확장(F2)

- F2-1: Pin 고정 잠금(실수로 움직이지 않게)
- F2-2: Pin 클릭 통과(Click-through) 모드
- F2-3: 단축키로 Pin 크기 100%/맞춤
- F2-4: 간단한 주석(사각형/화살표) — Phase 2 이후

## 7) 비기능 요구사항

- 성능: 핫키 입력 후 300ms 내 오버레이 표시 목표(환경에 따라 다름)
- 안정성: 권한 거부 시 가이드 및 안전한 실패 처리
- 보안/프라이버시: 캡처 이미지는 기본 로컬 처리, 외부 전송 없음
- 접근성: 키보드 중심 조작(최소한)

## 8) 권한/시스템 고려사항(macOS)

- 화면 캡처/녹화 권한(Screen Recording) 안내 UX 필요(첫 실행 또는 캡처 시).
- 전역 단축키는 Accessibility 권한이 필요할 수 있음(구현 방식에 따라).
- 샌드박스(App Store) 배포 시 저장 위치 및 권한, 엔타이틀먼트 설계 필요.

## 9) 기술 설계(권장 아키텍처)

### Option A: Swift + SwiftUI(AppKit 혼합) 권장

- 캡처 오버레이: NSWindow/NSPanel + 마우스 이벤트로 드래그 영역
- 이미지 캡처: CGDisplayStream / CGWindowListCreateImage / ScreenCaptureKit(버전 고려)
- Pin 창: NSPanel 사용, floating level 설정
- 글로벌 단축키: 라이브러리 또는 Carbon HotKey(주의: 최신 정책/권한 확인)

### Option B: Tauri/Electron

- UI는 웹으로 빠르게 가능
- 캡처/핀은 결국 네이티브 브릿지가 필요
- 권한/샌드박스/배포 난이도는 Swift보다 높을 수 있음

## 10) 데이터/설정 모델

- Settings
  - captureHotkey: String
  - closeAllHotkey: String
  - defaultOpacity: Float (0.3 ~ 1.0)
  - defaultBorder: Bool
  - defaultShadow: Bool
  - saveDirectoryBookmark: (App Sandbox용)
- PinWindowState(옵션)
  - lastFrame: CGRect
  - lastScreenID: String

## 11) 에러/엣지 케이스

- 권한 미부여: 설정 열기 버튼 제공 + “왜 필요한지” 1~2문장 설명
- 멀티 모니터: 오버레이/캡처 영역 좌표계 일관성(스케일/레티나 포함)
- 레티나 스케일: 픽셀/포인트 변환 주의
- 풀스크린 앱 위: 오버레이 표시가 제한될 수 있어 대체 UX 필요(알림/권한 안내)

## 12) 릴리즈 플랜

### Phase 1 (MVP, 1~2주)

- 오버레이 캡처 → Pin 생성
- 핫키 2개
- Copy / Close / Close All
- 기본 설정(투명도)

### Phase 2 (2~4주)

- Click-through / Lock
- Pin 정렬/스냅
- 저장/파일명 규칙
- 마지막 영역 재사용

### Phase 3 (선택)

- 주석(간단)
- 히스토리(최근 N개)
- Setapp/App Store 배포 최적화, 로컬라이징

## 13) 수익화/배포(선택)

- 오픈소스(MIT) + 팁(깃헙 스폰서) 또는
- 무료(MVP) + Pro 기능(Click-through/Lock/정렬/히스토리 등) 유료 업그레이드
- 배포 채널: GitHub Releases(.dmg/.zip), Homebrew cask, Mac App Store(정책/샌드박스 고려)

## 14) 성공 지표

- 핫키 → Pin 생성까지 평균 소요시간
- 하루 Pin 사용 횟수(세션당)
- “전체 Pin 닫기” 사용 비율(정리 UX 만족도 간접지표)
- 크래시율, 권한 이슈로 인한 이탈률

---

## 다음 액션

- MVP 기준으로 기능 우선순위 확정(F0/F1 중 무엇을 MVP에 넣을지)
- 앱 이름/아이콘 컨셉 결정
- 단축키 기본값 확정(시스템 단축키와 충돌 회피)
